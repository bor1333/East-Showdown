generic_ammo_silo_protection = {
    icon = generic_formable_nations

    priority = 10

    visible_when_empty = yes
    allowed = {
		always = yes
	}

    visible = {
		has_country_flag = himars_ammo_silo_striked
		NOT = {
			has_country_flag = himars_ammo_silo_protection
		}
    }
}

generic_casualties = {
	icon = GFX_generic_casualties

	visible = {
		OR = {
			has_war = yes
			check_variable = { casualties_k > 0 }
		}
	}

	visible_when_empty = yes

	priority = 205
}